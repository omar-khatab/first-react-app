// export this component to use it the page
export default function Mysecondcomponent() {
    return (
      // every component must have on tag, so we put all tags inside div or empty tag
      <div>
        <h1>My name is omar khatab</h1>
        <p>can do it</p>
        <button>press</button>
      </div>
    );
  }